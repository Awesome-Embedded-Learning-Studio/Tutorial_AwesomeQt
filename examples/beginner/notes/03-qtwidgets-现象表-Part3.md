# 03-qtwidgets 代码样例现象表 (Part 3: 38-55)

> 以下为各示例运行时的预期现象描述，供逐个核实时对照。

## 38-qgroupbox-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口整体布局 | 弹出一个 520x620 的窗口，标题为"QGroupBox 综合演示 -- 分组框"。全局 QSS 为所有 QGroupBox 设置了加粗标题、圆角边框、蓝色标题文字以及自定义勾选指示器样式。 |
| 2 | "基本设置"分组框（普通不可勾选） | 顶部显示一个带"基本设置"标题的分组框，内含：用户名输入框（带 placeholder "请输入用户名"）、界面语言下拉框（选项：简体中文/English/日本語/Deutsch）、"开机自动启动"复选框。该分组框不可勾选，始终启用。 |
| 3 | "网络配置（使用代理）"分组框（可勾选） | 中间显示一个带复选框的分组框，默认未勾选（内部控件灰色禁用状态）。勾选后：分组框内部启用，代理地址自动填入"127.0.0.1"，端口自动设为 8080；底部状态标签变为绿色背景，显示"代理已启用，默认地址 127.0.0.1:8080"。取消勾选后：代理地址清空，状态标签变为灰色背景，显示"代理已禁用"。 |
| 4 | "界面偏好"分组框（嵌套子分组框） | 显示一个标题居中的"界面偏好"分组框，内部嵌套两个子分组框：**"主题设置"** 内含"深色模式"和"跟随系统主题"（默认选中）两个复选框；**"字体设置"** 内含字体大小 SpinBox（范围 8-36，默认 14，后缀"px"）和字体族下拉框（Sans Serif/Serif/Monospace）。 |
| 5 | 底部状态标签 + 应用/重置按钮 | 窗口底部有灰色状态标签"修改设置后点击「应用」生效"、蓝色"应用"按钮和灰色"重置"按钮。点击**应用**：状态标签变蓝色背景，显示所有当前设置汇总（用户名、语言、自动启动、代理状态、深色模式、字体信息）。点击**重置**：所有控件恢复默认值，状态标签变橙色背景，显示"所有设置已重置为默认值"。 |

## 39-qtabwidget-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口整体布局 | 弹出一个 600x500 的窗口，标题为"QTabWidget 综合演示 -- 标签页控件"。全局 QSS 为标签页设置了圆角样式，选中标签高亮蓝色底边，悬停变浅蓝。 |
| 2 | 顶部控制栏 | 包含两个下拉框和两个复选框："标签栏位置"（上方/下方/左侧/右侧）、"标签形状"（圆角/三角形）、"可关闭"（默认勾选）、"可拖动"（默认勾选）。切换位置时标签栏移到对应方位；切换形状时标签外形改变；取消"可关闭"则标签上的关闭按钮消失；取消"可拖动"则标签不可拖动排序。 |
| 3 | 四个预设标签页 | **常规**页含用户名输入框、界面语言下拉框、开机自动启动复选框；**网络**页含代理地址输入框、端口号 SpinBox(默认 8080)、超时 SpinBox(默认 30)；**外观**页含深色模式复选框、跟随系统主题复选框（默认选中）、字体大小 SpinBox(默认 14px)；**高级**页默认灰色禁用，含调试模式和详细日志两个复选框及一条橙色警告文字。每个标签带有系统图标和 tooltip 提示。 |
| 4 | 标签切换与状态栏 | 点击不同标签时，底部状态标签实时更新，格式为"当前页面: 常规 (索引 0)"等。高级标签显示"[已禁用]"标记。 |
| 5 | 关闭标签页 | 点击标签上的关闭按钮，弹出 QMessageBox 确认对话框"确定关闭「xxx」标签页吗？"，选 Yes 则移除该标签页和页面内容。 |
| 6 | 解锁高级选项 | 勾选底部"解锁高级选项"复选框后，高级标签变为可用（文字和图标正常显示），tooltip 变为"高级设置"。取消勾选则重新禁用。 |
| 7 | 新增标签页 | 点击绿色"新增标签页"按钮，自动添加一个新标签页，内含居中信息标签和文本编辑框，自动切换到新标签页。底部状态栏同步更新。 |

## 40-qtabbar-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口整体布局 | 弹出一个 700x500 的窗口，标题为"QTabBar 综合演示 -- 独立标签栏"。演示 QTabBar 独立于 QTabWidget 使用，配合 QStackedWidget 和 QSplitter 实现类似编辑器的界面。 |
| 2 | 顶部独立标签栏 | 显示三个可关闭、可拖动的标签："README.md"、"main.cpp"、"CMakeLists.txt"。选中标签高亮为白色背景+蓝色底边，悬停变浅蓝。标签可拖动排序。 |
| 3 | 工具栏按钮 | 标签下方有绿色"新建文档"按钮和红色"关闭全部"按钮，右侧有灰色状态标签显示当前文档信息。 |
| 4 | 分割编辑区与预览区 | 中央区域水平分割：左侧 3:1 比例为 QTextEdit 编辑器，右侧为 QLabel 预览区。编辑器内容实时同步到预览区（纯文本转义后显示）。初始三个标签各有预设内容。 |
| 5 | 标签切换 | 点击不同标签时，编辑器和预览区同步切换到对应文档内容，状态标签更新为"文档: README.md \| 索引: 0 \| 总计: 3 个"格式。 |
| 6 | 关闭标签 | 点击标签关闭按钮，弹出确认对话框"确定关闭「xxx」吗？"。确认后删除标签、编辑器和预览页。所有标签关闭后状态标签显示"所有文档已关闭"。 |
| 7 | 标签拖动排序 | 拖动标签改变顺序后，编辑器堆叠和预览堆叠的页面顺序同步调整，内容保持对应。 |
| 8 | 新建文档 | 点击"新建文档"按钮，新增标签"untitled_N.txt"，创建空编辑器和"(空文档)"预览，自动切换到新标签。 |
| 9 | 关闭全部 | 点击"关闭全部"按钮，弹出确认对话框，确认后依次移除所有标签和对应页面。 |

## 41-qstackedwidget-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口整体布局 | 弹出一个 700x480 的窗口，标题为"系统偏好设置 -- 个人信息"。模拟 macOS 系统偏好设置界面，左侧 QListWidget 导航 + 右侧 QStackedWidget 内容区。 |
| 2 | 顶部快速跳转 QComboBox | 顶部有一个下拉框，选项为"个人信息/网络设置/外观偏好/快捷键/关于"，选择后直接跳转到对应页面，左侧导航同步高亮。 |
| 3 | 左侧导航列表 | QListWidget 宽度 140-180px，包含 5 个带图标的导航项。选中项蓝色高亮加粗，悬停灰色背景。点击导航项切换右侧页面和窗口标题。 |
| 4 | 个人信息页面 | 显示"个人信息"蓝色标题（底部蓝色分隔线）、用户名输入框和邮箱输入框。 |
| 5 | 网络设置页面 | 显示"网络设置"蓝色标题、启用代理复选框、代理地址输入框、端口 SpinBox(默认 8080)、连接超时 SpinBox(默认 30 秒)。 |
| 6 | 外观偏好页面 | 显示"外观偏好"蓝色标题、深色模式复选框、跟随系统主题复选框（默认选中）、字体大小 SpinBox(默认 14px)。 |
| 7 | 快捷键页面 | 显示"快捷键设置"蓝色标题和一条橙色背景提示框，说明该功能正在开发中，计划支持自定义快捷键、冲突检测、导入导出等。 |
| 8 | 关于页面 | 居中显示"系统偏好设置"大标题（蓝色加粗 20px）、"版本 1.0.0"、灰色描述框（QStackedWidget 演示信息）、"Copyright 2025 Tutorial AwesomeQt"版权文字。 |
| 9 | 底部状态栏 | 灰色圆角标签显示当前页面信息，格式为"当前页面: 个人信息 -- 管理用户名和邮箱地址 \| 第 1/5 页"。导航切换时实时更新。 |

## 42-qsplitter-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口整体布局 | 弹出一个 960x640 的 QMainWindow，标题为"QSplitter 综合演示 -- IDE 布局"。模拟 IDE 三区布局：外层水平分割（左侧文件树 \| 右侧垂直分割），右侧垂直分割（编辑区 \| 输出面板）。 |
| 2 | 分割手柄样式 | 分割手柄为灰色 3px 宽线条，悬停时变为蓝色(#4A90D9)。所有区域已禁止折叠(setCollapsible = false)。 |
| 3 | 左侧文件树 QTreeWidget | 显示一个项目文件树，根节点展开，包含：src/（含 main.cpp, widget.cpp, widget.h）、resources/（含 icon.png, style.qss）、CMakeLists.txt、README.md。默认宽度 200px。 |
| 4 | 编辑区 QTextEdit | 右上方为文本编辑器，带 placeholder 提示"// 在此编写代码...// 拖动分割线调整各区域大小 // 关闭窗口后分割比例会被保存"。 |
| 5 | 输出面板 QTextEdit（只读） | 右下方为只读输出面板，启动时自动输出 5 行日志："QSplitter 综合演示已启动"、"外层水平分割器..."、"内层垂直分割器..."、"所有区域已禁止折叠..."、"关闭窗口时分割比例自动保存到 QSettings"。 |
| 6 | 工具栏 | 顶部有不可移动的工具栏，包含"重置默认布局"和"打印当前 sizes()"两个按钮。 |
| 7 | 拖动分割线 | 可自由拖动水平和垂直分割手柄调整各区域大小。点击"打印当前 sizes()"按钮，在输出面板追加两行日志显示内外分割器的 sizes() 值。 |
| 8 | 重置默认布局 | 点击"重置默认布局"按钮，外层恢复[200, 700]，内层恢复[450, 150]，输出面板追加日志"布局已重置为默认值..."。 |
| 9 | 持久化 | 关闭窗口时，分割比例通过 QSettings 自动保存。下次启动时从 QSettings 恢复上次的分割比例。 |

## 43-qtoolbox-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口整体布局 | 弹出一个 800x520 的 QMainWindow，标题为"QToolBox 综合演示 -- 系统控制面板"。水平分割器：左侧 QToolBox 折叠面板（默认宽 240px）+ 右侧日志区（默认宽 560px）。 |
| 2 | QToolBox 四个面板 | 左侧折叠面板有 4 个分组："显示设置"（分辨率下拉框 1920x1080/2560x1440/3840x2160 + 刷新率 SpinBox 30-240 默认 60）、"网络配置"（IP 地址输入框默认 192.168.1.100 + 子网掩码输入框默认 255.255.255.0）、"声音设置"（音量滑块 0-100 默认 75 + 静音复选框）、"高级选项"（默认禁用/灰色不可点击）。 |
| 3 | 面板切换 | 点击 QToolBox 的标签栏切换当前展开的面板，同时只能展开一个面板。右侧日志区追加"用户切换到面板: xxx (索引 N)"。 |
| 4 | 高级选项面板禁用/启用 | 底部状态栏有"启用高级选项"复选框。默认不勾选，高级选项面板灰色不可展开。勾选后高级选项面板解锁（含信息标签、调试模式复选框、详细日志复选框），标签文字恢复为"高级选项"，日志追加"高级选项面板已解锁"。取消勾选则重新锁定。 |
| 5 | 日志区域 | 右侧只读 QTextEdit，启动时自动输出 3 行初始日志："QToolBox 综合演示已启动"、"左侧是 QToolBox 折叠面板，点击标题栏切换面板"、"高级选项面板当前已禁用，勾选底部复选框解锁"。 |
| 6 | 状态持久化 | 关闭窗口时通过 QSettings 保存当前展开的面板索引，下次启动时恢复。 |

## 44-qscrollarea-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口整体布局 | 弹出一个 520x420 的 QMainWindow，标题为"QScrollArea 综合演示 -- 消息日志查看器"。分为顶部输入栏、中间滚动区域、底部操作栏三部分。 |
| 2 | 自定义滚动条样式 | 垂直滚动条为 8px 宽、灰色圆角手柄、无上下箭头按钮的精简样式。QScrollArea 有浅灰边框和圆角。 |
| 3 | 初始系统消息 | 启动时滚动区域内自动显示 3 条灰色斜体系统消息："QScrollArea 综合演示已启动"、"输入文字点击发送，消息会自动追加到底部并滚动"、"滚动条样式已通过 QSS 自定义"。 |
| 4 | 发送用户消息 | 在顶部输入框输入文字后点击"发送"按钮或按回车，消息以蓝色气泡样式追加到滚动区域底部，格式为"[HH:mm:ss] 消息内容"。输入框自动清空，滚动区域自动滚到底部。底部消息计数加 1。 |
| 5 | 批量添加 | 点击"批量添加 20 条"按钮，一次性追加 20 条蓝色气泡消息（"批量测试消息 #1 -- 验证 QScrollArea 在快速追加内容时的滚动行为"...#20），滚动区域自动滚到底部，消息计数更新。 |
| 6 | 清空消息 | 点击"清空所有消息"按钮，移除所有用户消息和系统消息，消息计数归零，追加一条系统消息"所有消息已清空"。 |
| 7 | 消息计数标签 | 底部右侧显示"消息数: N"，实时反映当前用户消息数量。 |

## 45-qframe-separator-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口整体布局 | 弹出一个 680x520 的 QMainWindow，标题为"QFrame 分隔线综合演示"。演示 QFrame 作为水平分隔线(HLine)、垂直分隔线(VLine)、带标题分隔线、有边框容器的用法。 |
| 2 | 工具栏 VLine 分隔 | 顶部工具栏包含三组操作按钮："New"/"Open"/"Save" \| VLine \| "Cut"/"Copy"/"Paste" \| VLine \| "Help"/"About"。VLine 为灰色 Sunken 样式的垂直分隔线，将按钮组在视觉上分开。 |
| 3 | 左侧主内容区（带边框容器 + 标题分隔线） | 左侧是一个 StyledPanel+Raised 样式的 QFrame 容器，内含两个区域：**个人信息区**（"--- 个人信息 ---"标题分隔线 + 姓名输入框 + 邮箱输入框）和**工作信息区**（HLine 水平分隔线 + "--- 工作信息 ---"标题分隔线 + 部门输入框 + 职位输入框）。标题分隔线为"线 -- 文字 -- 线"的水平布局。 |
| 4 | 中间垂直分隔线 | 主内容区和右侧状态面板之间有一条 VLine 垂直分隔线。 |
| 5 | 右侧状态面板 | 右侧固定 160px 宽的 StyledPanel 容器，含"状态面板"标签、绿色"就绪"状态文字、HLine 分隔线、以及说明文字"QFrame 分隔线数量: 工具栏 VLine:2 区域 HLine:4 侧栏 VLine:1 总计:7 条"。 |
| 6 | 底部说明文字 | 左侧内容区底部有一条 HLine 分隔线和灰色说明文字，解释 QFrame 分隔线与 addSpacing 的区别。 |

## 46-qlistwidget-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口整体布局 | 弹出一个 480x540 的 QMainWindow，标题为"QListWidget 综合演示 -- 待办任务管理器"。分为顶部输入栏、中间列表、底部操作栏三部分。 |
| 2 | 预填充任务列表 | QListWidget 显示 5 条预填充任务，每条前面有复选框。优先级 1 的任务文字为红色（"完成 Qt 教程第 46 篇"、"部署测试环境"），优先级 2 的为橙色（"修复项目编译警告"、"代码审查 PR #42"），优先级 3 的为黑色（"更新 README 文档"）。 |
| 3 | 添加任务 | 在输入框输入文字后按回车或点击"添加任务"按钮，新任务追加到列表底部（默认优先级 3，黑色文字），输入框清空，列表自动滚到底部。 |
| 4 | 勾选完成 | 勾选任务复选框后，该任务文字变为灰色并添加删除线效果。取消勾选则恢复原优先级颜色。底部状态标签实时更新已完成数量。 |
| 5 | 删除选中 | 支持多选(ExtendedSelection)，点击"删除选中"按钮移除所有选中条目。 |
| 6 | 清除已完成 | 点击"清除已完成"按钮，移除所有已勾选（checkState == Checked）的条目。 |
| 7 | 双击编辑 | 双击任务条目弹出 QInputDialog 对话框，可修改任务名称。 |
| 8 | 右键上下文菜单 | 右键点击任务条目弹出菜单，可设置"高优先级"（红色）、"中优先级"（橙色）、"普通优先级"（黑色），或"删除此任务"。 |
| 9 | 底部状态文字 | 实时显示"总计: N  已完成: M  选中: K"，随操作自动更新。 |

## 47-qlistview-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口整体布局 | 弹出一个 640x480 的 QMainWindow，标题为"QListView 综合演示 -- 颜色列表"。水平布局：左侧 QListView + 右侧控制面板(200px 宽)。 |
| 2 | 颜色列表（列表模式） | QListView 默认为 ListMode，使用 QStringListModel 存储 12 个颜色名称（Tomato, SteelBlue, SeaGreen, Gold, Orchid, Coral, Turquoise, Salmon, MediumPurple, LimeGreen, DodgerBlue, HotPink）。通过自定义 ColorItemDelegate 绘制：每行左侧显示颜色名称文本，右侧绘制一个 14x14 的圆角颜色色块。 |
| 3 | 切换图标模式 | 点击"图标模式"按钮，视图切换为 IconMode，图标大小变为 64x64，ColorItemDelegate 改为在每格中心绘制一个大颜色圆（带阴影），下方显示颜色名称文字(9pt)。gridSize SpinBox 变为可用。 |
| 4 | 切换列表模式 | 点击"列表模式"按钮，恢复为 ListMode，图标大小 24x24，gridSize 清空，gridSize SpinBox 禁用。 |
| 5 | 间距控制 | 调整 Spacing SpinBox(0-30) 可改变列表项之间的间距。 |
| 6 | 网格大小控制 | 在图标模式下，调整 GridSize SpinBox(60-200) 可改变每个网格的尺寸，颜色圆和文字随之缩放。 |
| 7 | 添加颜色 | 在输入框输入有效的 CSS 颜色名称（如"RoyalBlue"）后点击"添加"或回车，颜色被追加到列表末尾。输入无效颜色名时输入框边框变红。 |
| 8 | 颜色预览 | 选中列表中的颜色后，右侧控制面板底部的预览区域背景变为该颜色，文字显示颜色名称。浅色背景配深色文字，深色背景配浅色文字。 |
| 9 | 状态标签 | 底部显示"条目数: N \| 模式: 列表/图标"。 |

## 48-qtreewidget-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口整体布局 | 弹出一个 640x500 的 QMainWindow，标题为"QTreeWidget 综合演示 -- 项目文件浏览器"。水平布局：左侧三列树表 + 右侧信息/操作面板(220px 宽)。 |
| 2 | 三列树表结构 | QTreeWidget 有 3 列表头："名称"/"类型"/"大小"。根节点"QtProject"（项目/--）默认展开，下含 src/ 文件夹（含 main.cpp 2.1KB, widget.h 1.8KB, widget.cpp 5.6KB）、include/ 文件夹（含 widget.h 0.9KB, helper.h 0.9KB）、resources/ 文件夹（含 icon.png 24.3KB, style.qss 3.1KB）、CMakeLists.txt（构建文件 1.2KB）、README.md（Markdown 0.5KB）。 |
| 3 | 选中节点显示路径 | 点击树中任意节点，右侧"当前路径"标签显示从根到当前节点的完整路径，如"QtProject / src / main.cpp"。 |
| 4 | 展开/折叠日志 | 展开或折叠节点时，右下角状态标签显示"已展开: xxx"或"已折叠: xxx"。 |
| 5 | 单击节点日志 | 单击节点时状态标签显示"点击: xxx (列 N)"。 |
| 6 | 双击编辑节点名称 | 双击节点弹出 QInputDialog 对话框，可修改节点名称。修改后路径显示同步更新。 |
| 7 | 添加节点 | 在"节点名称"输入框输入名称后，点击"添加文件夹"或"添加文件"按钮。若有选中节点则添加为子节点，否则添加为顶层节点。父节点自动展开。 |
| 8 | 删除节点 | 点击"删除选中节点"按钮，弹出 QMessageBox 确认对话框。若节点有子节点，提示"该节点包含 N 个子节点，将一并删除"。确认后删除节点，路径标签显示"未选中"。 |

## 49-qtreeview-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口整体布局 | 弹出一个 720x520 的 QMainWindow，标题为"QTreeView 综合演示 -- 文件浏览器"。使用 QFileSystemModel + QTreeView 展示真实文件系统。 |
| 2 | 顶部路径导航栏 | 显示"路径:"标签 + QLineEdit（默认填入用户主目录路径）+ "跳转"按钮。输入有效路径后按回车或点击跳转，文件树根节点切换到该路径，底部显示"已跳转: /xxx"。输入不存在的路径时输入框边框变红，显示"路径不存在: /xxx"。 |
| 3 | 文件树视图 | QTreeView 显示用户主目录下的文件和文件夹（隐藏了大小/类型/修改日期列，只显示文件名列）。只显示真实文件系统内容。 |
| 4 | 右侧文件信息面板 | 点击文件树中的文件或文件夹后，右侧信息面板显示详细信息：名称、路径、类型（文件夹/文件）、子项数（文件夹）或文件大小（文件）、后缀名、修改时间。 |
| 5 | 展开控制按钮 | 底部有四个按钮："展开全部"（expandAll 展开所有节点）、"折叠全部"（collapseAll）、"展开选中"（expand 当前选中节点）、"折叠选中"（collapse 当前选中节点）。 |
| 6 | 展开/折叠状态 | 展开或折叠节点时，底部状态标签显示"已展开: xxx"或"已折叠: xxx"。 |

## 50-qtablewidget-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口整体布局 | 弹出一个 720x480 的 QMainWindow，标题为"QTableWidget 综合演示 -- 学生信息管理"。水平布局：左侧 6 列表格 + 右侧详情面板(200px 宽)。 |
| 2 | 学生数据表格 | QTableWidget 有 6 列（学号/姓名/性别/年龄/专业/成绩），预填充 7 条学生数据：张三(男/20/计算机科学/92.5)、李四(女/21/软件工程/88.0)、王五(男/19/人工智能/95.0)、赵六(女/20/数据科学/78.5)、钱七(男/22/信息安全/85.0)、孙八(女/20/计算机科学/91.0)、周九(男/21/软件工程/76.5)。行选模式，学号列不可编辑，其他列双击可编辑。 |
| 3 | 右侧详情面板 | 点击表格中任意行，右侧详情面板显示该学生的完整信息（学号/姓名/性别/年龄/专业/成绩）。 |
| 4 | 编辑提示 | 编辑单元格内容后，右侧下方显示"已修改: (行, 列)"提示。 |
| 5 | 底部统计标签 | 左下方灰色标签显示"总计: N 名学生 \| 平均成绩: XX.X"，编辑成绩后自动重新计算。 |
| 6 | 添加学生 | 点击"添加学生"按钮，弹出 QInputDialog 输入姓名，自动生成学号，在表格末尾新增一行（性别默认"未知"、年龄默认 20、专业默认"未指定"、成绩默认 0.0）。 |
| 7 | 删除学生 | 选中一行后点击"删除选中行"按钮，弹出 QMessageBox 确认"确定删除学生「xxx」?"。确认后移除该行，统计和详情同步更新。 |

## 51-qtableview-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口整体布局 | 弹出一个 800x520 的 QMainWindow，标题为"QTableView 综合演示 -- 员工管理"。使用 QStandardItemModel + QSortFilterProxyModel + QTableView 实现。 |
| 2 | 顶部搜索与筛选栏 | 包含"搜索姓名:"输入框和"部门:"下拉框（全部/研发部/产品部/设计部/运维部）。输入关键词实时过滤姓名列（模糊匹配），选择部门后按部门精确过滤。注意：二者共用一个 proxy model，实际只能生效最后设置的条件。 |
| 3 | 员工数据表格 | 6 列（编号/姓名/部门/职位/工龄/绩效），预填充 10 条员工数据。编号列不可编辑。开启排序功能(setSortingEnabled)，点击表头可按列排序。部门列有 setSpan 合并（同部门行合并显示）。 |
| 4 | 列宽策略 | 编号/姓名/部门/职位/工龄列自适应内容宽度，绩效列拉伸填满剩余空间。 |
| 5 | 选中行高亮 | 选中某行后，底部统计标签追加显示"选中: 姓名 (部门)"。 |
| 6 | 底部统计标签 | 显示"共 N 人 \| 平均工龄: X.X 年"，随过滤条件变化实时更新。 |

## 52-qheaderview-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口整体布局 | 弹出一个 900x520 的 QMainWindow，标题为"QHeaderView 综合演示 -- 商品清单"。水平布局：左侧 QTableView + 右侧控制面板(220px 宽)。 |
| 2 | 自定义彩色表头 | 使用 ColoredHeaderView（继承 QHeaderView），表头每列左侧有一个彩色方块标记（蓝/绿/橙/红/紫/青），背景为灰色渐变，文字加粗。排序指示列右侧显示三角箭头（升序向上/降序向下）。 |
| 3 | 商品数据表格 | 6 列（编号/名称/分类/单价/库存/备注），预填充 8 条商品数据（机械键盘、4K显示器、无线鼠标、扩展坞、降噪耳机、便携SSD、人体工学椅、桌面台灯）。编号列不可编辑。支持排序。 |
| 4 | 列宽策略切换 | 右侧有四个按钮：**Fixed**（固定宽度，编号 70/名称 160/分类 70/单价 70/库存 60/备注 160）、**ResizeToContents**（自适应内容宽度）、**Stretch**（所有列等比拉伸）、**Interactive**（用户可手动拖拽调整列宽）。 |
| 5 | 列可见性控制 | 右侧有 6 个复选框（编号/名称/分类/单价/库存/备注），默认全部勾选。取消勾选则隐藏对应列，重新勾选则显示。 |
| 6 | 排序控制 | 右侧有下拉框选择排序列 + "升序排列"/"降序排列"按钮。点击后 proxy model 执行排序，表头排序指示箭头同步更新。 |

## 53-qcolumnview-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口整体布局 | 弹出一个 960x560 的 QMainWindow，标题为"QColumnView 综合演示 -- 产品分类浏览器"。模仿 macOS Finder 列视图风格。 |
| 2 | 多列级联视图 | QColumnView 以多列形式展示三层产品分类树。点击第一层的分类节点（电子产品/服装/食品），右侧展开第二列显示子分类；点击子分类，再展开第三列显示具体产品。三列宽度分别为 160/160/180px。 |
| 3 | 预览面板 | 最右侧有一个预览面板(260px 宽)。选中分类节点时显示分类名称、类型"分类节点"、子项数量、深度层数；选中具体产品时显示产品名称、分类、价格、品牌、描述等详细信息。 |
| 4 | 产品数据 | 三大类共约 16 个产品：**电子产品**（手机：iPhone 16 Pro/Pixel 9 Pro/Galaxy S25；笔记本：MacBook Pro 14/ThinkPad X1 Carbon；音频：AirPods Pro 3/WH-1000XM6）、**服装**（上装：羊绒毛衣/冲锋衣；下装：西裤/牛仔裤；鞋类：NB 990v6/ASICS Gel-Kayano 31）、**食品**（零食：黑巧克力/混合坚果；饮品：冷萃咖啡/气泡水；生鲜：蓝莓/和牛）。 |
| 5 | 面包屑导航 | 底部灰色圆角标签显示从根到当前选中项的路径，格式如"电子产品 > 手机 > iPhone 16 Pro"。随点击实时更新。 |

## 54-qundoview-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口整体布局 | 弹出一个 900x560 的 QMainWindow，标题为"QUndoView 综合演示 -- 文档编辑器"。水平分割(3:1)：左侧编辑器+操作按钮 + 右侧撤销历史视图。 |
| 2 | 工具栏 | 顶部有不可移动的工具栏，包含撤销（左箭头图标，Ctrl+Z）和重做（右箭头图标，Ctrl+Shift+Z）两个按钮，由 QUndoStack 创建标准 Action。 |
| 3 | 只读编辑器 | 左侧 QPlainTextEdit 设为只读，placeholder 提示"在此输入文本，或使用下方按钮执行操作..."。 |
| 4 | 操作按钮栏 | 编辑器下方有五个按钮："插入示例文本"、"追加一行"、"删除末尾行"、"清空全部"、"保存"。 |
| 5 | 插入示例文本 | 点击后追加三行文本（含递增计数器），QUndoStack 记录"插入 \"...\""命令，QUndoView 列表中新增一条记录。 |
| 6 | 追加一行 | 每次点击追加一句古诗（循环 6 句：春眠不觉晓/夜来风雨声/白日依山尽/欲穷千里目/锄禾日当午/谁知盘中餐），QUndoView 记录。 |
| 7 | 删除末尾行 | 删除编辑器最后一行文本，QUndoView 记录"删除 \"...\""。编辑器为空时无效。 |
| 8 | 清空全部 | 清空编辑器所有文本，QUndoView 记录一条删除命令。编辑器为空时无效。 |
| 9 | QUndoView 历史面板 | 右侧列表显示所有操作历史，每条显示命令描述（如"插入 \"这是第 1 次...\""）。选中历史条目可跳转到该时间点（undo/redo 到对应状态）。初始显示"(无操作)"。保存点旁显示保存图标。 |
| 10 | 保存与 clean 状态 | 点击"保存"按钮调用 setClean()，窗口标题恢复无星号，状态标签显示绿色"已保存"。执行任何新操作后标题追加"*"，状态标签变红色"未保存"。 |
| 11 | 撤销/重做 | 通过工具栏按钮或 Ctrl+Z/Ctrl+Shift+Z 撤销/重做操作，编辑器内容和 QUndoView 同步更新。 |

## 55-qmainwindow-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口整体布局 | 弹出一个 1024x640 的 QMainWindow，标题为"QMainWindow 综合演示 -- 文本编辑器"。完整的 IDE 风格主窗口，包含菜单栏、工具栏、中央编辑器、Dock 窗口和状态栏。 |
| 2 | 中央编辑器 | QPlainTextEdit 作为中央控件，placeholder 提示列出了所有演示功能点。可自由编辑文本。 |
| 3 | 菜单栏 -- 文件菜单 | "文件(&F)"菜单含：新建(Ctrl+N)、打开(Ctrl+O)、保存(Ctrl+S)、分隔线、退出(Ctrl+Q)。前三个带系统图标和状态栏提示文字，退出直接关闭窗口。 |
| 4 | 菜单栏 -- 编辑菜单 | "编辑(&E)"菜单含：撤销(Ctrl+Z)、重做(Ctrl+Shift+Z)、分隔线、剪切(Ctrl+X)、复制(Ctrl+C)、粘贴(Ctrl+V)、分隔线、全选(Ctrl+A)。所有操作连接到编辑器对应槽函数。 |
| 5 | 菜单栏 -- 视图菜单 | "视图(&V)"菜单含：大纲/属性/输出三个 Dock 窗口的切换 Action（勾选/取消可显示/隐藏对应 Dock）、分隔线、"恢复默认布局"操作。 |
| 6 | 菜单栏 -- 帮助菜单 | "帮助(&H)"菜单含"关于(&A)"，点击后状态栏显示临时消息"QMainWindow 综合演示 v1.0 -- Qt 6.9.1"，3 秒后消失。 |
| 7 | 文件工具栏 | 第一行工具栏含新建、打开、保存三个带图标的按钮。 |
| 8 | 编辑工具栏 | 第二行工具栏含撤销（左箭头图标）和重做（右箭头图标）按钮。 |
| 9 | 状态栏 | 状态栏左侧显示"就绪"临时消息（3 秒后消失）。右侧永久显示两个标签："行 N, 列 M"（光标位置，随编辑实时更新）和"字符: N"（字符统计，随编辑实时更新）。 |
| 10 | 大纲 Dock 窗口（左侧） | QDockWidget"大纲"，内含一个无表头的 QTreeView。允许停靠在左侧或右侧。 |
| 11 | 输出 Dock 窗口（底部） | QDockWidget"输出"，内含只读 QPlainTextEdit（最大高度 150px），预填文字"应用程序启动完成。\n窗口布局已从 QSettings 恢复。"允许停靠在底部或顶部。 |
| 12 | 属性 Dock 窗口（右侧） | QDockWidget"属性"，内含标签"字体: Monospace\n大小: 14pt\n编码: UTF-8"。与大纲 Dock 标签化合并(tabifyDockWidget)，大纲 Dock 默认在前台。 |
| 13 | 恢复默认布局 | 视图菜单中的"恢复默认布局"操作会移除所有 Dock 并按默认位置重新添加，窗口大小重置为 1024x640，状态栏显示"已恢复默认布局"2 秒。 |
| 14 | 窗口状态持久化 | 关闭窗口时通过 QSettings 保存窗口几何信息(geometry)和布局状态(windowState)。下次启动时自动恢复上次的窗口大小、位置和 Dock 布局。 |
