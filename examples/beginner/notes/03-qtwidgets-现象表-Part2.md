# 03-qtwidgets 代码样例现象表 (Part 2: 19-37)

> 以下为各示例运行时的预期现象描述，供逐个核实时对照。

## 19-qradiobutton-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口 SettingsWidget | 弹出标题为 "QRadioButton 综合演示 -- 应用设置" 的窗口，600x420。全局 QSS 将 QRadioButton 的 indicator 设为圆形（蓝色选中、灰色未选中），QGroupBox 有圆角边框 |
| 2 | 左侧 "主题选择" 分组 | 三个单选按钮："浅色主题"（默认选中）、"深色主题"、"跟随系统"。同属一个 QGroupBox parent，自动互斥，只能选一个 |
| 3 | 右上 "语言选择" 分组 | 两个单选按钮："中文"（默认选中）、"English" |
| 4 | 右下 "字体大小" 分组 | 三个单选按钮："小 (12px)"、"中 (14px)"（默认选中）、"大 (16px)" |
| 5 | "toggled(bool) 信号监听" 分组 | 灰色背景文字区域，切换任意主题单选按钮时显示如 "浅色主题 选中 -> 深色主题 取消选中" 的日志链，最多保留 4 条 |
| 6 | 底部状态栏 | 实时显示如 "主题: 浅色 \| 语言: 中文 \| 字体: 中"，切换选项后自动更新 |
| 7 | "重置为默认值" 按钮 | 点击后恢复浅色/中文/中号，清空信号日志区显示 "已重置为默认值" |

## 20-qcheckbox-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口 CheckBoxDemo | 弹出标题为 "QCheckBox 综合演示 -- 三态 & 层级复选" 的窗口，640x560。QSS 将 QCheckBox indicator 设为圆角方形（蓝色选中、浅蓝半选） |
| 2 | "全选 / 全不选 (三态复选框)" 分组 | 一个三态 "全选" 复选框 + 五个子项（选项 A-E）。勾选 "全选" 则全选所有子项；取消全选则全部取消；子项部分选中时 "全选" 变为半选状态（PartiallyChecked，浅蓝填充） |
| 3 | "层级复选 (QTreeWidget)" 分组 | 树形控件，包含 "项目组 Alpha" 和 "项目组 Beta" 两个顶层节点，各含 main.cpp / config.json / README.md 三个子节点。勾选父节点向下传播（子节点全选/全不选），子节点变化向上冒泡更新父节点状态（全选/半选/未选） |
| 4 | 底部状态栏 | 灰色背景标签实时显示如 "复选框组: 3/5 已选中 \| 树形结构: 4/6 已选中" |

## 21-qcommandlinkbutton-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口 ProjectWizard | 弹出标题为 "QCommandLinkButton 综合演示 -- 项目创建向导" 的窗口，560x520。全局 QSS 将 QGroupBox 圆角化 |
| 2 | 三个 QCommandLinkButton | 位于 "项目模板" 分组内，各带图标、主标题和描述文字：(1) "空项目" + 文件图标 (2) "Qt Widgets 应用" + 电脑图标 (3) "Qt Quick 应用" + 详细视图图标。QSS 设置蓝色文字、悬浮时浅蓝背景 + 边框 |
| 3 | 结果展示区 | 初始显示 "等待选择..."，点击任一按钮后更新为 "已选择: XXX\n点击「下一步」继续配置" |
| 4 | 底部操作日志 | 显示 "最近操作: 选择「XXX」 [HH:mm:ss]"，带时间戳 |
| 5 | "退出" 按钮 | 点击关闭窗口 |

## 22-qlineedit-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口 RegisterForm | 弹出标题为 "QLineEdit 综合演示 -- 用户注册表单" 的窗口，480x560。QSS 将 QLineEdit 设为圆角边框，聚焦时蓝色边框 |
| 2 | 用户名输入框 | placeholder 为 "字母、数字、下划线，最长 20 字符"，右侧有清除按钮，最大长度 20。旁边实时显示字符计数 "X / 20" |
| 3 | 邮箱输入框 | placeholder 为 "example@domain.com"，带清除按钮，正则验证 |
| 4 | 年龄输入框 | placeholder 为 "1 - 150"，QIntValidator 限制范围 |
| 5 | 密码输入框 | echoMode 为 Password，输入内容显示为圆点，带清除按钮 |
| 6 | 确认密码输入框 | echoMode 为 Password。失焦时自动检测密码一致性，结果区域显示 "两次密码不一致"（红色）或 "密码一致"（绿色） |
| 7 | "注册" 按钮 | 蓝色背景白色文字。点击后执行完整验证：空值检查、用户名长度>=3、邮箱格式、年龄范围、密码长度>=6、密码一致性。成功时结果区显示绿色 "注册成功！用户: XXX"；失败时显示红色错误列表 |
| 8 | 信号日志区域 | 一个 QLineEdit + 深色背景日志标签 + "程序 setText(\"hello\")" 按钮。手动输入时同时触发 textChanged 和 textEdited；点击按钮设置文字时只触发 textChanged 不触发 textEdited；按回车或失焦触发 editingFinished |

## 23-qtextedit-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口 MiniEditor | 弹出标题为 "QTextEdit 综合演示 -- 迷你文本编辑器" 的窗口，640x580 |
| 2 | 格式工具栏 | "B 加粗"、"I 斜体"、"U 下划线"、"插入蓝色标题"、"清除所有格式" 五个按钮 |
| 3 | 编辑区域初始内容 | QTextEdit 显示 HTML 富文本内容，包含 h2 标题、加粗/斜体/下划线文字 |
| 4 | 选中文字 + 格式按钮 | 选中文字后点击加粗/斜体/下划线按钮，选中部分应用对应格式 |
| 5 | "插入蓝色标题" 按钮 | 在光标位置插入蓝色加粗 14pt 文字 "\n蓝色标题文字\n" |
| 6 | "清除所有格式" 按钮 | 将内容转为纯文本再设回，所有富文本格式被清除 |
| 7 | 状态栏 | 左侧实时显示 "字符: N" 和 "行数: N"；右侧显示 "状态: 未修改" 或 "状态: 已修改 *"（红色加粗） |
| 8 | 修改状态追踪 | 编辑内容后窗口标题追加 " *" 标记，状态栏变红；关闭窗口时弹出 QMessageBox 询问是否保存 |

## 24-qplaintextedit-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口 LogTerminal | 弹出标题为 "QPlainTextEdit 综合演示 -- 模拟日志终端" 的窗口，680x520 |
| 2 | 控制面板 | "开始日志"、"停止日志"（初始禁用）、"清空日志" 三个按钮 + 行数上限下拉框（100/500/1000/无限制，默认 1000） |
| 3 | 日志显示区 | HighlightPlainTextEdit（只读、等宽字体、不自动换行），光标所在行有浅蓝背景高亮。初始包含 "[INFO] 日志终端已就绪" 和 "[INFO] 点击「开始日志」按钮启动模拟输出" |
| 4 | 开始日志 | 点击后每 300ms 追加一条随机日志，格式为 "[HH:mm:ss.zzz] [INFO/WARN/ERROR] 随机消息"，自动滚动到底部。"开始日志" 禁用，"停止日志" 启用 |
| 5 | 停止日志 | 点击后定时器停止，追加 "[INFO] 日志输出已暂停"，按钮状态恢复 |
| 6 | 清空日志 | 点击后清空所有日志内容 |
| 7 | 行数上限 | 切换下拉框改变 setMaximumBlockCount，超出上限时自动丢弃最早的日志行 |
| 8 | 底部状态栏 | 显示 "行数: N" 和 "上限: XXXX 行" |

## 25-qtextbrowser-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口 HelpBrowser | 弹出标题为 "QTextBrowser 综合演示 -- 迷你帮助文档浏览器" 的窗口，680x560。程序创建 QTemporaryDir 写入 5 个 HTML 文件 |
| 2 | 导航工具栏 | "<< 后退"（初始禁用）、"前进 >>"（初始禁用）、"首页"（初始禁用）三个按钮 + "当前: index.html" 标签 |
| 3 | 初始页面 | QTextBrowser 加载 index.html，显示 "MiniHelp 帮助文档" 标题、四个超链接（简介/使用指南/常见问题/关于） |
| 4 | 页面导航 | 点击超链接跳转到对应 HTML 页面，"后退" 按钮启用；点击 "后退" 返回上一页，"前进" 按钮启用。导航按钮的启用/禁用由 backwardAvailable/forwardAvailable 信号自动驱动 |
| 5 | 外部链接处理 | 点击 "Qt 官方文档" 等外部链接时，QDesktopServices::openUrl() 在系统默认浏览器中打开，QTextBrowser 重新加载当前页面 |
| 6 | "当前:" 标签 | sourceChanged 信号更新，实时显示当前页面文件名（如 "当前: intro.html"） |

## 26-qkeysequenceedit-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口 ShortcutConfigWindow | QMainWindow，标题为 "QKeySequenceEdit 综合演示 -- 快捷键配置面板"，600x520。顶部有菜单栏 "操作(A)" 包含 "新建"、"保存"、"关闭" |
| 2 | 快捷键配置表单 | 三个 QKeySequenceEdit 分别显示默认快捷键：新建(Ctrl+N)、保存(Ctrl+S)、关闭(Ctrl+W) |
| 3 | 修改快捷键 | 点击 QKeySequenceEdit 后按下新按键组合，keySequenceChanged 信号触发。应用新快捷键到 QAction，日志区记录 "[配置] XXX 快捷键已更新为: YYY" |
| 4 | 快捷键冲突检测 | 如果新快捷键与其他操作重复，弹出 QMessageBox 警告 "快捷键冲突"，恢复原来的快捷键 |
| 5 | 菜单/快捷键触发 | 通过菜单或快捷键触发操作时，日志区记录 "[触发] XXX"（如 "[触发] 新建"） |
| 6 | "恢复默认快捷键" 按钮 | 一键恢复所有快捷键为 Ctrl+N/Ctrl+S/Ctrl+W，日志记录 "[配置] 所有快捷键已恢复为默认值" |
| 7 | 触发日志区 | QPlainTextEdit（只读，最多 50 行），等宽字体，显示带时间戳的操作日志 |

## 27-qcombobox-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口 CitySelectorPanel | 弹出标题为 "QComboBox 综合演示 -- 城市信息选择器" 的窗口，600x520 |
| 2 | "只读下拉 (addItem + userData)" | QComboBox 包含 "全部城市"（通过 insertItem 插入到索引 0）+ 北京/上海/广州/深圳/杭州/成都（各带 userData 编码）。右侧蓝色标签显示 "城市编码: ALL"，切换后更新为对应编码 |
| 3 | "可编辑下拉 (addItems + setEditable)" | QComboBox 可编辑，addItems 批量添加城市，输入框有 placeholder。右侧标签显示 "当前输入: XXX"（在列表中时黑色）或 "自定义输入: XXX"（不在列表中时红色） |
| 4 | "自定义 Model (QStandardItemModel)" | QComboBox 使用 QStandardItemModel，每个 item 存储城市名（显示）、编码（UserRole）和地区（UserRole+1）。右侧显示 "编码: XX \| 地区: XX" |
| 5 | 操作日志 | 底部 QPlainTextEdit（只读）记录所有操作，格式 "[HH:mm:ss] 只读选择框 -> 北京 (编码: BJ)" |

## 28-qfontcombobox-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口 FontPreviewTool | 弹出标题为 "QFontComboBox 综合演示 -- 字体预览工具" 的窗口，720x480，左右两栏布局 |
| 2 | 左侧控制面板 (280px 固定宽度) | QFontComboBox（字体选择）、QComboBox（过滤器：全部/等宽/比例/可缩放字体）、QComboBox（书写系统：任意/简体中文/日文/韩文/拉丁文）、QComboBox（字号 8-72，默认 14）、粗体/斜体复选框 |
| 3 | 右侧预览区 | QTextEdit 显示英文字母、数字符号、中文诗词等多语言示例文本。字体随左侧控件实时变化 |
| 4 | 字体过滤器切换 | 选择 "等宽字体" 后 QFontComboBox 只显示等宽字体；选择 "简体中文" 后只显示支持中文的字体 |
| 5 | 状态标签 | 显示 QFontInfo 信息："family: XXX\nstyleName: XXX\npointSize: 14 \| bold: false \| italic: false" |

## 29-qspinbox-doublespinbox-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口 ImageExportPanel | 弹出标题为 "QSpinBox / QDoubleSpinBox 综合演示 -- 图像导出设置" 的窗口，680x480，左右两栏布局 |
| 2 | 左侧 "尺寸设置" 分组 | 宽度 QSpinBox（1-4096，步长 10，后缀 " px"，默认 1920）、高度 QSpinBox（默认 1080）、"锁定宽高比" 复选框（默认勾选） |
| 3 | 锁定宽高比联动 | 勾选时修改宽度自动按 16:9 比例调整高度（blockSignals 防循环），解锁后独立调整 |
| 4 | 左侧 "导出参数" 分组 | DPI QSpinBox（72-1200，步长 72，后缀 " DPI"，默认 300）、缩放 QDoubleSpinBox（0.1-5.0，步长 0.1，1 位小数，后缀 " x"，默认 1.0）、质量 QSpinBox（1-100，步长 5，后缀 " %"，默认 85） |
| 5 | 右侧 "导出设置摘要" | 蓝色背景标签实时显示如 "1920 x 1080 px -> 导出 1920 x 1080 px\nDPI: 300 \| 缩放: 1.0x \| 质量: 85%\n预估文件大小: ~6.3 MB\n宽高比锁定" |
| 6 | 右侧 "操作日志" | QPlainTextEdit（只读），记录数值变化如 "[HH:mm:ss] 宽度 -> 1930 px" |

## 30-qdatetimeedit-dateedit-timeedit-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口 SchedulePanel | 弹出标题为 "QDateEdit / QTimeEdit / QDateTimeEdit 综合演示 -- 日程安排" 的窗口，720x540 |
| 2 | "日程信息" 表单 | 标题 QLineEdit（placeholder "输入日程标题..."）、开始日期 QDateEdit（弹出日历，格式 "yyyy 年 MM 月 dd 日"，范围今天~1 年后）、开始时间 QTimeEdit（格式 "HH:mm"，范围 06:00-23:00，默认 09:00）、截止时间 QDateTimeEdit（弹出日历，格式 "yyyy-MM-dd HH:mm"，默认当天 18:00） |
| 3 | "时长摘要" 区域 | 绿色背景标签，实时显示如 "从 2026-05-04 09:00 到 2026-05-04 18:00\n持续时长: 9 小时"。截止时间早于开始时间时显示红色警告 |
| 4 | "添加日程" 按钮 | 蓝色背景白色文字。标题为空或截止时间不合法时弹出 QMessageBox 警告；验证通过后在 QListWidget 添加一条如 "[2026-05-04] 开会 \| 09:00 -> 05-04 18:00 \| 9h 0m" |
| 5 | 已添加的日程列表 | QListWidget，选中项蓝色高亮 |

## 31-qslider-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口 ColorPalettePanel | 弹出标题为 "QSlider 综合演示 -- 颜色调色板" 的窗口，700x480，左右两栏 |
| 2 | 左侧 "颜色通道 (RGB)" 分组 | 三个水平 QSlider（范围 0-255，默认 128），QSS 自定义外观：R 为红色 handle + 红色 sub-page，G 为绿色，B 为蓝色。各带刻度线和数值标签 |
| 3 | 左侧 "参数设置" 分组 | 透明度 QSlider（0-100，默认 80%）和笔刷 QSlider（1-50，默认 10px），灰色风格 QSS |
| 4 | 右侧 "颜色预览" | 大面积色块，背景色随 RGB 滑块实时变化（如 #808080），中间显示 HEX 值（黑字或白字自适应亮度） |
| 5 | 右侧 "颜色信息" | 显示 "HEX: #808080\nRGB: rgb(128, 128, 128)\n透明度: 80%" |
| 6 | 右侧 "信号追踪" | 拖动 R 滑块时显示 "sliderMoved(R): XXX"；松开时显示 "sliderReleased(R), 最终值: XXX" |

## 32-qscrollbar-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口 TimelineBrowser | 弹出标题为 "QScrollBar 综合演示 -- 自定义时间轴浏览器" 的窗口，800x480。深色顶部信息栏、中央时间轴、底部水平滚动条、底部状态栏 |
| 2 | 顶部信息栏 | 深灰背景(#263238)，左侧显示 "项目时间轴"，右侧显示 "缩放: 1.0x \| 内容宽度: XXX px" |
| 3 | 中央 TimelineWidget | 自定义绘制的项目时间轴，显示 13 个事件卡片（不同颜色），包括 "项目启动"、"需求分析"、"UI 设计"、"后端开发" 等。底部有时间轴基线和日期刻度（每 5 天显示 "Day N"） |
| 4 | 垂直滚动条（右侧） | 控制缩放（0.5x - 3.0x），默认 1.0x。拖动可放大/缩小时间轴卡片和日期宽度 |
| 5 | 水平滚动条（底部） | 控制水平偏移，驱动时间轴左右滚动。鼠标滚轮也可触发水平滚动（通过 eventFilter 转发） |
| 6 | 底部状态栏 | 浅灰背景，显示 "偏移: 0 \| 缩放: 1.0x \| 视口: XXX x XXX" |
| 7 | 窗口 resize | 自动重新计算水平滚动条范围（setPageStep = 视口宽度） |

## 33-qdial-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口 DashboardWidget | 弹出标题为 "QDial 综合演示 -- 汽车仪表盘" 的窗口，640x400 |
| 2 | "速度表" 旋钮 | QDial（0-240，步长 10，不 wrapping，显示刻度，140x140）。下方蓝色大字显示 "0 km/h"，拖动旋钮实时更新 |
| 3 | "转速表" 旋钮 | QDial（0-8000，步长 500，不 wrapping，显示刻度，140x140）。下方橙色大字显示 "0 RPM" |
| 4 | "温度表" 旋钮 | QDial（40-120，步长 5，不 wrapping，显示刻度，初始值 40）。下方大字颜色随温度变化：<80 蓝色（冷车状态，正在预热）、80-100 绿色（温度正常）、>100 红色（温度过高，请注意冷却系统！） |
| 5 | "角度旋钮（setWrapping = true）" 分组 | QDial（0-359，步长 15，wrapping=true，显示刻度，120x120）。拧过 359 直接回到 0，旁边紫色大字显示 "XXX 度" |
| 6 | 底部状态栏 | 显示 "当前操作: XXX" 或温度状态描述 |

## 34-qlabel-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口 LabelDemoWidget | 弹出标题为 "QLabel 综合演示 -- 文本与图像显示" 的窗口，600x700 |
| 2 | "文本显示" 分组 -- 纯文本 | 显示 "纯文本：Hello, Qt! 当前版本 6.9.1"，setTextFormat(PlainText) |
| 3 | "文本显示" 分组 -- 代码片段 | 深色背景(#263238)青色等宽字体显示 "代码片段：std::vector<int> vec = {1, 2, 3};"，setTextFormat(PlainText) 避免尖括号被解析为 HTML |
| 4 | "文本显示" 分组 -- HTML 富文本 | 蓝色 h3 标题、加粗/斜体/下划线文字、超链接 "Qt 官方文档"、列表。点击链接底部显示 "点击了链接: https://doc.qt.io"；悬停显示 "悬停链接: https://doc.qt.io" |
| 5 | "对齐与换行" 分组 | 上排三个标签分别左对齐、居中、右对齐。下方固定宽度 400px 的自动换行标签，演示中文长文本 wordWrap 效果 |
| 6 | "setBuddy 伙伴控件" 分组 | "姓名(&N):" + QLineEdit、"邮箱(&E):" + QLineEdit、"年龄(&A):" + QSpinBox。按 Alt+N/E/A 可快速跳转到对应输入框，QLabel 中 &字母显示为带下划线的快捷键标记 |
| 7 | "图片显示" 分组 | 程序用 QPainter 生成 120x120 蓝色占位图和 60x60 缩放版本，通过 setPixmap 显示。旁边文字说明 |
| 8 | 底部信息栏 | 显示 "就绪"、"点击了链接: XXX" 或 "悬停链接: XXX" |

## 35-qprogressbar-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口 ProgressDemoWidget | 弹出标题为 "QProgressBar 综合演示 -- 跨线程进度更新" 的窗口，520x480。QSS 设置蓝色进度条块 + 圆角 |
| 2 | "基本进度条" 分组 | 两条进度条：(1) 百分比格式 "处理进度: 0%" (2) 详细值格式 "已处理 0 个，共 100 个"。下方标签 "当前值: 0 / 100" |
| 3 | "无限进度条 (setRange(0, 0))" 分组 | 初始隐藏。开始处理后显示，条内无文字，持续左右滚动动画表示忙碌状态 |
| 4 | "开始处理" 按钮 | 蓝色按钮。点击后创建 Worker 对象并 moveToThread 到新线程。按钮禁用，"取消" 按钮启用，状态变蓝 "处理中... (工作线程运行中)"，无限进度条出现 |
| 5 | 跨线程进度更新 | Worker 每步 sleep 80ms，通过 progressChanged(int) 信号跨线程（QueuedConnection）更新两条进度条。约 8 秒完成 100 步 |
| 6 | "取消" 按钮 | 红色按钮。点击后 Worker 设置 atomic 标志，线程在下一步检查后停止，进度条停在取消位置，状态变橙 "已取消（停在第 N 步）" |
| 7 | 处理完成 | 两条进度条到 100%，状态变绿 "处理完成！"，无限进度条 1.5 秒后自动隐藏 |

## 36-qlcdnumber-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口 LcdDemoWidget | 弹出标题为 "QLCDNumber 综合演示 -- 仪表盘数字显示" 的窗口，560x520。QSS 设置深蓝黑背景(#1a1a2e)绿色(#00ff88) LCD 数字 |
| 2 | 主 LCD 显示器 | QLCDNumber（8 位，Flat 样式，最小高度 80px），初始显示 0 |
| 3 | "整数输入 + 进制切换" 分组 | QSlider（0-9999）+ 数值标签 + QComboBox（十进制/十六进制/八进制/二进制）。拖动滑块 LCD 实时更新数值；切换进制后 LCD 按对应进制显示（自动调整 digitCount：Dec=8, Hex=6, Oct=8, Bin=16） |
| 4 | "浮点数输入 + setSmallDecimalPoint" 分组 | QDoubleSpinBox（-999.99~999.99，2 位小数）+ "SmallDecimalPoint: OFF" 切换按钮。修改浮点数值时 LCD 显示小数。开启 SmallDecimalPoint 后小数点占位变小 |
| 5 | "仪表盘计时器 (QTimer + QLCDNumber)" 分组 | "启动计时器"（蓝色）和 "重置"（初始禁用）两个按钮。启动后 QTimer 每 1 秒递增，LCD 显示 "HH:MM:SS" 格式计时，按钮变红 "停止计时器" |
| 6 | overflow 检测 | 当显示内容超过 digitCount 位数时，状态标签变红提示 "overflow: 显示位数不足，请增大 digitCount" |

## 37-qcalendarwidget-beginner

| 序号 | 演示要点 | 预期现象 |
|------|----------|----------|
| 1 | 主窗口 CalendarDemoWidget | 弹出标题为 "QCalendarWidget 综合演示 -- 日历控件" 的窗口，720x480，左右两栏布局。QSS 设置日历导航栏浅蓝背景(#E3F2FD) |
| 2 | 左侧 QCalendarWidget | 日历控件，可选日期范围 2020-01-01 至 2030-12-31，显示网格线，单选模式。表头红色加粗，周六/周日灰色文字 |
| 3 | 右侧 "当前选中日期" | 蓝色背景大字标签显示如 "2026-05-04 周一"；QDateEdit 手动输入日期（带日历弹出），与日历双向同步 |
| 4 | "可选日期范围" 分组 | 最小日期 QDateEdit（默认 2020-01-01）、最大日期 QDateEdit（默认 2030-12-31）、"应用范围约束" 按钮。若最小>最大则红色错误提示 |
| 5 | "日期高亮" 分组 | 三个按钮："高亮当前月份周末"（绿色按钮，周末日期红底红字加粗）、"高亮今天"（橙色按钮，今天黄底加粗）、"清除所有高亮"（灰色按钮，清除当月自定义格式） |
| 6 | 底部状态标签 | 显示 "点击日历选择日期"、"当前页面: 2026 年 05 月" 等操作反馈 |
| 7 | 月份切换 | currentPageChanged 信号更新状态标签显示当前年月 |
